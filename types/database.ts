// NOTE: Generate proper types by running in terminal:
// npx supabase gen types typescript --project-id usjyuccastvhhiwhdhvq --schema public > types/database.ts
//
// Until then, use a permissive type to avoid build errors.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
